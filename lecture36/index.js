// strict mode
// => Strict mode in JavaScript is a feature that enables a stricter parsing and error handling for your code.
// => When you enable strict mode, it helps you write cleaner code and catch errors that might otherwise go unnoticed.

"use strict";
a = 10;
console.log(a);
